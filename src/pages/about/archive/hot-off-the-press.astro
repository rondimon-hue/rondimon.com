---
import Page from '../../../layouts/Page.astro';

const title = `Hot off the press`;
const dateStr = "March 27, 2013";
const content = `<p><a href="http://goo.gl/o4yIX"><img class="alignright size-medium wp-image-51" alt="first copy" src="http://rondimon.com/wp-content/uploads/2013/03/first-copy.jpeg?w=217" width="217" height="300" /></a>I just got my advance copy.  It's gratifying to finally hold the thing in my hands and feel it.  It should reach Amazon's warehouse on Monday April 1 (no joke!)</p>

<p>In the meantime, if you just can't wait, the <a href="http://www.amazon.com/dp/B00BR02GR2">kindle</a> and <a href="http://itun.es/i6Jv48J">iBook</a> versions are available now.</p>`;
---

<Page title={title} description={`Archived blog post from rondimon.com — ${dateStr}`}>

  <section class="container py-20 md:py-28">
    <div class="max-w-3xl animate-in">
      <a href="/about/archive" class="inline-flex items-center text-sm text-text-muted hover:text-accent transition-colors mb-8">
        <svg class="w-4 h-4 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7" />
        </svg>
        Archive
      </a>
      <p class="text-sm text-text-muted mb-4">{dateStr}</p>
      <h1 class="text-3xl md:text-4xl font-heading font-bold text-text-primary">
        {title}
      </h1>
    </div>

    <div class="mt-12 max-w-3xl prose-archive" set:html={content} />
  </section>

</Page>
